@text
.LDI $0 #1
.LDI $1 #0x41 ; A

:wait_key
.JZF :wait_key $0 ; wait until keyboard code writes keycode into $0

.JEQ :got_a $0 $1 ; if Key = A, exit success

.LDI $0 #0
.JMP :wait_key

:got_a
.LDI $0 #0
.HLT
